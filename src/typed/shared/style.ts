// A thin, deliberate convenience re-export, checked against what a future reader genuinely needs before writing anything here: document-content-model's own Alignment/AlignmentSchema already IS ODF's paragraph-alignment vocabulary one-for-one (see src/styles/properties.ts's fo:text-align mapping, which uses the identical four values -- left/center/right/justify -- with no ODF-specific translation step at all), so re-exporting it here saves a future reader from having to remember whether "ODF alignment" comes from this package or from document-content-model directly. Every other "style" concern a reader needs -- the property-bag shape and its parse/build/canonicalize functions (src/styles/properties.ts, src/styles/serialize.ts), style-name interning (src/styles/registry.ts), span-wrapping (src/styles/span.ts), and the default-style/parent-chain cascade (this directory's own cascade.ts) -- already exists elsewhere with nothing left to add here. This file staying a one-line re-export is the correct, complete outcome for this phase, not an oversight to be padded out.
export { type Alignment, AlignmentSchema } from 'document-schema.js';
